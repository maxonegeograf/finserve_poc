# Copy to secrets.toml (same folder) and set your key: https://aistudio.google.com/apikey
# Streamlit loads this automatically — do not commit secrets.toml to git.

GEMINI_API_KEY = "your_key_here"

# Optional (default gemini-2.5-flash)
# GEMINI_MODEL = "gemini-2.5-flash"
